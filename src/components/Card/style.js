import styled from 'styled-components';

export const Container = styled.div`
    width: 100%;
    heidht: 220px;

    position: fixed;
    bottom: 0;

    display: flex;
    align-items: center;
    justify-content: center;
`